- add open rx scan mode
- add search wave form (from windows driver)
- FIXME listeners won't timeout unless they're invoked
  to process a message, causes ugly applciation hang
  if something breaks.
- add methods / callbacks for sending data over wireless
- ant_serial_dialect.py
  - if assertion is raised during message tokenization, thread will die
  - matcher, don't trap IndexError, add an AntError type for "unimplemented message"
